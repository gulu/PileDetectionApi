# 2026-05-25

## Git / GitHub 初始化
- 在 GitHub 上创建了公开仓库 `gulu/PileDetectionApi`
- 本地初始化 Git 仓库，默认分支重命名为 `main`
- 提交并推送了所有代码（96 个文件，2 次提交）
- .gitignore 配置了 .NET 标准排除规则（bin/、obj/、packages/等）
- .gitattributes 配置了换行符规范化规则
- 注意：远程 URL 中嵌入了 token（`https://gulu:token@github.com/...`），如需更安全的凭证方式后续可改用 SSH 或 Git Credential Manager

## 账号-项目权限控制功能
- 新增 `ProjectPermissionEntity` → `project_permission` 表（多对多关联 api_key ↔ project_info）
- 新增 `POST /api/v1/projects/permitted-list` 端点（从 JWT 提取 clientId 过滤）
- 新增 `IProjectService.GetPermittedPagedAsync` + `ProjectService.GetPermittedPagedAsync`
- 新增 5 个单元测试，全部通过
- 修复 `AdminAuthService.cs` 中 catch 块引用不存在变量 `expiresAt` 的编译错误
- 更新 `build.bat`/`test.bat` 使用 `C:\dotnet8\dotnet.exe` 绝对路径
- 更新 `global.json`: SDK 8.0.421，rollForward: latestPatch
- 更新 `nuget.config`: 包缓存路径改为 `C:\Users\Administrator\.nuget\packages`

## .NET SDK 环境修复
- 系统 SDK 10.0.300 在 NuGet restore 时报 `Value cannot be null (Parameter 'path1')`
- 使用 dotnet-install.ps1 安装了 .NET 8 SDK (8.0.421) 到 `C:\dotnet8`
- NuGet 包还原使用 nuget.exe 单独执行
- 构建/测试使用 `C:\dotnet8\dotnet.exe --no-restore` 模式

## 项目权限分配管理接口
- 新增 `AdminProjectPermissionController` 管理员控制器
- 4 个端点：`POST /api/v1/admin/project-permissions`（授予）、`/batch`（批量）、`/list`（列表）、`/{id}/revoke`（撤销）
- 所有端点通过 `X-Master-Key` 头鉴权
- 新增 DTO: `GrantPermissionRequest`, `BatchGrantPermissionRequest`, `ListPermissionsRequest`, `ProjectPermissionResponse`
- 新增 7 个单元测试（23/23 全部通过）
