# 2026-05-25

## Git / GitHub 初始化
- 在 GitHub 上创建了公开仓库 `gulu/PileDetectionApi`
- 本地初始化 Git 仓库，默认分支重命名为 `main`
- 提交并推送了所有代码（96 个文件，2 次提交）
- .gitignore 配置了 .NET 标准排除规则（bin/、obj/、packages/等）
- .gitattributes 配置了换行符规范化规则
- 注意：远程 URL 中嵌入了 token（`https://gulu:token@github.com/...`），如需更安全的凭证方式后续可改用 SSH 或 Git Credential Manager
