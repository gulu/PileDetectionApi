# 2026-05-11 工作记录

## 完成事项
1. **安装 .NET 8.0 SDK** (8.0.420) 到 `C:\dotnet8`
2. **创建 `global.json`** 锁定 SDK 版本
3. **修复 NuGet restore 问题** - 在 Git Bash 中 `dotnet restore` 因环境变量问题失败，所有命令使用 `--no-restore` 规避
4. **修复 SeedDataTests** - 添加 `[Trait("Category", "Integration")]`，修复硬编码 ID 查询
5. **修复中文乱码** - Program.cs 添加 `Console.OutputEncoding = Encoding.UTF8`
6. **创建 test.bat** - 快速测试运行脚本
7. **更新 build.bat** - 简化构建流程，使用 `--no-restore`
8. **创建 nuget.config** - 项目级 NuGet 配置
9. **验证 11 个单元测试全部通过** - 无乱码、无错误

## 集成测试修复
- **问题**：`PileService.GetDetailAsync` 使用 FreeSql `IncludeMany(p => p.ProfileStats)` + `Include(p => p.Report)` 生成无效 SQL（`near "WHERE": syntax error`）
- **修复**：改用独立查询加载 ProfileStats、Report 和 Measurements
- **验证**：`dotnet test --no-restore --filter "SeedDataTests"` ✅ 通过

## ER 图生成
- 创建了 `dbdata/er-diagram.html`（浏览器直接打开查看）
- 创建了 `dbdata/er-diagram.mmd`（Mermaid 源文件）
- 7 个实体：project_info, pile_info, profile_stat, measurement_data, pile_report, project_report, api_log
- 5 个关系：project→pile(1:N), project→project_report(1:N), pile→profile_stat(1:N), pile→measurement_data(1:N), pile→pile_report(1:1)

## API Key 管理安全改造
### 新增文件
- `Entities/ApiKeyEntity.cs` - api_key 数据表实体
- `Services/AdminAuthService.cs` + `IAdminAuthService.cs` - 管理端密钥生成服务
- `Controllers/AdminAuthController.cs` - 管理 API 端点
- `DTOs/Request/AdminRequest.cs` + `Response/AdminResponses.cs` - 管理端 DTO
- `Configs/AdminConfig.cs` - MasterKey 配置

### 修改文件
- `Services/AuthService.cs` - 从硬编码改为查数据库验证
- `Data/FreeSqlSetup.cs` - 新增 ApiKeyEntity 同步，首次启动自动创建默认 Key
- `Program.cs` - 注册 AdminAuthService + AdminConfig
- `appsettings.json` - 新增 Admin:MasterKey + Kestrel 端口配置
### 配置迁移
- `launchSettings.json` 的端口配置移至 `appsettings.json` 的 Kestrel 节点
## API 文档生成
- 创建了 `dbdata/api-documentation.md` - 第三方接口文档
