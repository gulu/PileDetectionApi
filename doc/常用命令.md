cd D:/admodel/pile/dev

# 1. 启动 API（FreeSql 自动建表）
dotnet run --project src/PileDetectionApi

# 2. 单元测试（内存 SQLite，不写磁盘）
dotnet test --filter "Category!=Integration"

# 3. 整体功能测试（读取 xlsx 灌入正式数据到 dbdata/pile.db）
dotnet test --filter "SeedDataTests"


# 方式 1：完整命令 不做依赖的更新/还原
目的	命令
运行集成测试（灌入数据）	dotnet test --no-restore --filter "SeedDataTests"
运行单元测试（不灌入数据）	dotnet test --no-restore --filter "Category!=Integration"
运行全部测试	dotnet test --no-restore



cd  D:\admodel\pile\dev\src\PileDetectionApi

dotnet run .\PileDetectionApi.csproj 

http://localhost:5000/swagger